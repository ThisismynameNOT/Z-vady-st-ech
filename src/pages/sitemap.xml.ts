import type { APIRoute } from 'astro'; import { getProjects, getSite } from '../lib/content'; export const GET:APIRoute=({url})=>{const s=getSite();const base=(import.meta.env.SITE_URL||s.siteUrl||url.origin).replace(/\/$/,'');const paths=['/','/firma/','/sluzby/','/realizace/','/reference/','/kontakt/','/ochrana-osobnich-udaju/',...getProjects().map(p=>`/realizace/${p.slug}/`)];const xml=`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${paths.map(p=>`<url><loc>${base}${p}</loc></url>`).join('')}</urlset>`;return new Response(xml,{headers:{'Content-Type':'application/xml; charset=utf-8'}})};

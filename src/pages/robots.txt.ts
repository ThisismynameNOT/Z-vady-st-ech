import type { APIRoute } from 'astro'; import { getSite } from '../lib/content'; export const GET:APIRoute=({url})=>{const site=getSite();const base=(import.meta.env.SITE_URL||site.siteUrl||url.origin).replace(/\/$/,'');return new Response(`User-agent: *\nAllow: /\nDisallow: /admin/\nDisallow: /api/\nSitemap: ${base}/sitemap.xml\n`,{headers:{'Content-Type':'text/plain; charset=utf-8','Cache-Control':'public, max-age=3600'}})};
